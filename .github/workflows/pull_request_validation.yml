name: Pull Request Validation

on: 
  push:
  pull_request:
    branches:
    - development
jobs:
  frontend-build:
    name: Frontend Build
    runs-on: ubuntu-latest
    steps: 
      - name: Build Angular Application
        run: npm run ng build
